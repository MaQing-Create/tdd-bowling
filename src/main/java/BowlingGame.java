public class BowlingGame {
    int calculateTotalScore(int[] bowlingGameScoreRecord) {
        if (bowlingGameScoreRecord.length != 21)
            throw new RuntimeException("There is something with bowling game score record!");
        return 0;
    }
}
